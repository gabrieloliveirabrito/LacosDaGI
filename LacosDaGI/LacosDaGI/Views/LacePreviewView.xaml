<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:LacosDaGI.Models"
             x:Class="LacosDaGI.Views.LacePreviewView">
    <NavigationPage.TitleView>
        <Label FontSize="21" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="Laços da GI - " />
                    <Span Text="{Binding Lace.Name}" />
                </FormattedString>
            </Label.FormattedText>
        </Label>
    </NavigationPage.TitleView>
    <ContentPage.BindingContext>
        <models:LacePreviewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            
            <Image MinimumWidthRequest="200" MinimumHeightRequest="200" Aspect="Fill" Grid.Row="0" Grid.ColumnSpan="2" Source="{Binding Lace.Image, Converter={StaticResource Base64ToImage}}" />
            
            <Label Grid.Row="1" Grid.Column="0" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand">Nome do laço</Label>
            <Label Grid.Row="1" Grid.Column="1" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding Lace.Name}"/>
            
            <Label Grid.Row="2" Grid.Column="0" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand">Nome do laço</Label>
            <Label Grid.Row="2" Grid.Column="1" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding Lace.Name}"/>
            
            <Label Grid.Row="3" Grid.Column="0" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand">Nome do laço</Label>
            <Label Grid.Row="3" Grid.Column="1" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding Lace.Name}"/>
            
            <Label Grid.Row="4" Grid.Column="0" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand">Nome do laço</Label>
            <Label Grid.Row="4" Grid.Column="1" FontAttributes="Bold" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding Lace.Name}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>